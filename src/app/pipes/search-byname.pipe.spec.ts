import { SearchBynamePipe } from './search-byname.pipe';

describe('SearchBynamePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchBynamePipe();
    expect(pipe).toBeTruthy();
  });
});
