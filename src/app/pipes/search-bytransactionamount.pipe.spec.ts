import { SearchBytransactionamountPipe } from './search-bytransactionamount.pipe';

describe('SearchBytransactionamountPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchBytransactionamountPipe();
    expect(pipe).toBeTruthy();
  });
});
